import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ThinkFlow — Chat + Canvas + Anchored Exploration",
  description:
    "A conversational AI canvas where every piece of generated content can be selected, questioned, and recursively explored through context-aware branches while preserving your original path.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased dark`}
    >
      <body className="h-full flex flex-col bg-zinc-950 text-white overflow-hidden">
        {children}
      </body>
    </html>
  );
}
